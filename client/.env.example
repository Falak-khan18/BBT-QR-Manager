# Browser-readable API origin (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Must match backend REDIRECT_BASE_URL — encoded inside generated QR images.
# For phone scans from another device, use the same reachable host as the API (LAN IP or tunnel).
NEXT_PUBLIC_REDIRECT_BASE_URL=http://localhost:8000
